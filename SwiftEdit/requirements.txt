# SwiftEdit inference — stack mới (Python 3.12, Colab T4 / CUDA 12+).
# Không pin torch: dùng torch có sẵn trên Colab hoặc cài theo platform (Mac/CUDA).

transformers>=4.46.0,<5
diffusers>=0.32.2,<0.36
huggingface-hub>=0.27.0,<1
accelerate>=1.2.0
peft>=0.13.0
safetensors>=0.4.0
ftfy
tensorboard
Jinja2
einops
packaging
