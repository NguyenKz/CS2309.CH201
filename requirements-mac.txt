# CS2309 — SwiftEdit trên Mac (MPS). Cài sau khi tạo .venv:
#   pyenv local 3.12.10 && python -m venv .venv && source .venv/bin/activate
#   python -m pip install -r requirements-mac.txt

torch
torchvision
torchaudio
transformers>=4.46.0,<5
diffusers>=0.32.2,<0.36
huggingface-hub>=0.27.0,<1
accelerate>=1.2.0
peft>=0.13.0
safetensors>=0.4.0
ftfy
tensorboard
Jinja2
einops
numpy>=1.26.0,<3
pillow
jupyter
ipywidgets
matplotlib
packaging
# Demo UI (giữ <6 để không kéo huggingface-hub lên 1.x phá transformers)
gradio>=5,<6
pandas
